# <%= dsl.props[:package_name].camelize %> API

Welcome to the <%= dsl.props[:package_name].camelize %> gem! This gem provides the API to the <%= dsl.props[:package_name].camelize %> server.

## Installation

Add this line to your application's Gemfile:

```ruby
gem '<%= dsl.props[:package_name].to_s %>'
```

And then execute:

```
$ bundle install
```

Or install it yourself as:

```
$ gem install <%= dsl.props[:package_name].to_s %>
```

## Usage

You will need to require the gem

```ruby
require '<%= dsl.props[:package_name].to_s %>'
```

To connect to the server you will need to go

```
client = <%= dsl.props[:package_name].camelize %>.Client.new('https://<server location>')
```

## CLI

Installing gives you access to the CLI

You can use the CLI to interact with your server

### GET

```bash
<%= dsl.props[:package_name].to_s %> https://<server location> get <name_of_resource> [id]
```

Example:


```bash
<%= dsl.props[:package_name].to_s %> https://myserver.com get song 1KG6X86JVX74FDZ8VT2K2WXJ1
```


### POST

```bash
<%= dsl.props[:package_name].to_s %> https://<server location> post <name_of_resource> --field1 fieldvalue ...
```

Example:

```bash
<%= dsl.props[:package_name].to_s %> https://myserver.com post song --artist 'linkedin park' --name 'breaking the intern'
```


## Development

After checking out the repo, run `bin/setup` to install dependencies. Then, run `rake spec` to run the tests. You can also run `bin/console` for an interactive prompt that will allow you to experiment.

To install this gem onto your local machine, run `bundle exec rake install`. To release a new version, update the version number in `version.rb`, and then run `bundle exec rake release`, which will create a git tag for the version, push git commits and tags, and push the `.gem` file to [rubygems.org](https://rubygems.org).

## Contributing

Bug reports and pull requests are welcome on GitHub at <%= (dsl.props[:package_source_uri] || dsl.props[:package_homepage]) %>. This project is intended to be a safe, welcoming space for collaboration, and contributors are expected to adhere to the [code of conduct](<%= (dsl.props[:package_source_uri] || dsl.props[:package_homepage]) %>/blob/master/CODE_OF_CONDUCT.md).


## License

The gem is available as open source under the terms of the [MIT License](https://opensource.org/licenses/<%= dsl.props[:package_license] %>).

## Code of Conduct

Everyone interacting in the Base project's codebases, issue trackers, chat rooms and mailing lists is expected to follow the [code of conduct](<%= (dsl.props[:package_source_uri] || dsl.props[:package_homepage]) %>/blob/master/CODE_OF_CONDUCT.md).
